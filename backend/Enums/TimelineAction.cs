namespace backend.Enums
{
    public enum TimelineAction
    {
        Created,
        Assigned,
        Reassigned,
        StatusChanged,
        PriorityChanged,
        Resolved,
        Closed,
        Reopened,
        EvidenceUploaded,
        Upvoted
    }
}
